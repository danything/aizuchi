apiVersion: v2
name: slack-claude-bot
description: >-
  Interactive Slack bot backed by the Claude API (Claude Console API key).
  Socket Mode, so no ingress is needed; replies stream into a thread.
  Native AOT single binary.
type: application
# version / appVersion は CI が上書きする(helm package --version/--app-version)
version: 0.1.0
appVersion: dev
home: https://github.com/danything/slack-claude-bot
sources:
  - https://github.com/danything/slack-claude-bot
